#!/usr/bin/env sh

alias cat='bat'      # override the default cat command with bat
alias grep='batgrep' # override the default grep command with batgrep
alias man='batman'   # override the default man command with batman
