#!/usr/bin/env sh

alias be='bundle exec'                          # run a bundle exec command
alias bi='bundle install'                       # run bundle install
alias bu='bundle update'                        # run bundle update
alias bx='bundle exec'                          # run a bundle command with bundle exec
alias bxcop='bundle exec rubocop'               # run rubocop
alias bxcopA='bundle exec rubocop -A'           # run rubocop with unsafe auto-correction
alias bxcopa='bundle exec rubocop -a'           # run rubocop with safe auto-correction
alias bxdoc='bundle exec yard'                  # run yard doc
alias bxsdoc='bundle exec yard server --reload' # run yard server
alias bxspec='bundle exec rspec'                # run rspec
alias bxtc='bundle exec steep check'            # run steep check
alias killrb='killall -9 ruby'                  # kill all ruby processes
alias killruby='killrb'                         # kill all ruby processes
