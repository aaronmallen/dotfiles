# My personal dotfiles

## Installation

To install the dotfiles package run the following in your terminal app:

```bash
curl -Lks http://bit.ly/aa-macosx-cfg-install-latest | /bin/bash
bin/setup
```
